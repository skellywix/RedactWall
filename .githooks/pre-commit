@echo off
setlocal

where npm >nul 2>nul
if errorlevel 1 (
  echo [pre-commit] npm not found. Install Node.js and npm, then retry.
  exit /b 1
)

call npm run review:agent
if errorlevel 1 (
  echo [pre-commit] review:agent failed. Commit aborted.
  exit /b 1
)
