node_modules/
*.log
.env
.env*.local
# runtime data store — created at startup; never commit (DB artifacts, secrets,
# and legacy JSON that may hold cleartext PII)
data/
backups/
dist/
output/
# built console bundle — generated from console/ by `npm run console:build`
server/public/app/
test-results/
playwright-report/
# design-parity screenshots produced by e2e/console-design.spec.js
e2e/design-evidence/
# cloud-sync (ProtonDrive/FUSE) leftovers — never source
.fuse_hidden*
# editor/OS noise
.DS_Store
*.swp
